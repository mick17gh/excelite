/** Stored when a role is intentionally saved with zero permissions */
export const ROLE_PERMISSION_EMPTY_MARKER = "_none_";

/** Cookie set by middleware so layouts can read the current pathname */
export const REQUEST_PATHNAME_COOKIE = "ss-pathname";
